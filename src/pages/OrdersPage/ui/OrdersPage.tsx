import { useTranslation } from 'react-i18next';

const OrdersPage = () => {
    const { t } = useTranslation();

    return (
        <div>
            {12}
        </div>
    );
};

export default OrdersPage;
